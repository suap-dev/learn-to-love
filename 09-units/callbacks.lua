callbacks = {}

function callbacks.beginCallback(bodyA, bodyB, collision)
end

function callbacks.endCallback(bodyA, bodyB, collision)
end

function callbacks.preSolve(bodyA, bodyB, collision)
end

function callbacks.postSolve(bodyA, bodyB, collision)
end