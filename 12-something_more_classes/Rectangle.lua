Class = require 'class'

Rectangle = Class{
    init = function (self, x, y, r, g, b, o)
        self.x = x
        self.y = y
        self.r = r
        self.g = g
        self.b = b
        self.o = o
    end

    
}